Create a virtual environment and install all packages `pip install -r <path-to-requirements.txt>`.
If you use the collab, install missing packages using `!pip install <package-name>` in the code cell.
